// Define the structure for colors in the theme
export type Colors = {
    background: string;
    text: string;
    primary: string;
    secondary: string;
  };

// Define the theme structure with colors
export type Theme = {
  colors: Colors;
};
